<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InteriorQuote">

        <activity
            android:name=".QuoteActivity"
            android:exported="false" />
        <activity
            android:name=".AddEditFloorActivity"
            android:exported="false" />
        <activity
            android:name=".AddEditWindowActivity"
            android:exported="false" />
        <activity
            android:name=".RoomDetailActivity"
            android:exported="false" />
        <activity
            android:name=".AddEditRoomActivity"
            android:exported="false" />
        <activity
            android:name=".HouseDetailActivity"
            android:exported="false" />
        <activity
            android:name=".AddEditHouseActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="au.edu.utas.pborana.interiorquote.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>